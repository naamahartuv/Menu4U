<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.planshop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ContactUs"></activity>
        <activity android:name=".CurrentEvent" />
        <activity android:name=".CurrentRecipe" />
        <activity android:name=".AddEvent" />
        <activity android:name=".AdminEventList" />
        <activity android:name=".MemberEventList" />
        <activity android:name=".AddRecipe" />
        <activity android:name=".ActivitiesMenu" />
        <activity android:name=".AdminRecipes" />
        <activity android:name=".signUp" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>